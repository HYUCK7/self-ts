//JS DOCS - 자바스크립트를 타입스크립트로 검수하기 : allowJs
//@ts-check
/**
 * Initializes the Project
 * @param {object} config 
 * @param {boolean} config.debug
 * @param {string} config.url
 * @returns {boolean}
 */
export function init(config) {
    return true
}
/**
 * Exits the Program
 * @param {number} code 
 * @returns 
 */
export function exit(code){
    return code + 1
}